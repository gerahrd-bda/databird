import pandas as pd
import plotly.express as px
from dash import Dash, dcc, html

"""
My interactive 3D Dashboard takes you on a ride through the data, offering a fresh perspective on those dry numbers. 
It’s not perfect, but it sure makes spotting trends and outliers easier and more fun. 
It’s a handy tool that adds a bit of flair to data analysis, though it's still just a glimpse of what’s beneath the surface.
"""

# Define the updated data for the DataFrame (three times bigger)
data = {
    'A': [3, 6, 9, 12, 15],
    'B': [6, 12, 18, 24, 30],
    'C': [9, 18, 27, 36, 45]
}

# Create a DataFrame
df = pd.DataFrame(data)

# Calculate key metrics
mean_values = df.mean()
std_dev_values = df.std()

# Create a 3D scatter plot using Plotly Express
fig_3d = px.scatter_3d(df, x='A', y='B', z='C', color='C', title="3D Scatter Plot of DataFrame")

# Create a Dash app
app = Dash(__name__)

app.layout = html.Div([
    html.H1("Interactive 3D Dashboard"),
    html.Div([
        html.H2("3D Scatter Plot"),
        dcc.Graph(figure=fig_3d)
    ]),
    html.Div([
        html.H2("Key Metrics"),
        html.P(f"Mean Values: {mean_values.to_dict()}"),
        html.P(f"Standard Deviation: {std_dev_values.to_dict()}")
    ])
])

if __name__ == '__main__':
    app.run_server(debug=True)
